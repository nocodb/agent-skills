# Changelog

## 1.0.0

- Initial release for Cursor, Grok Build, and Claude Code.
- Bundles the hosted NocoDB MCP server (`https://app.nocodb.com/mcp`, OAuth) for record operations.
- Bundles the `nocodb` skill: a CLI for the NocoDB v3 REST API (workspaces, bases, tables, fields, views, records, links, attachments, team management).
